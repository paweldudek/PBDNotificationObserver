platform :ios, '6.0'

inhibit_all_warnings!

source 'https://github.com/CocoaPods/Specs'

podspec path: 'PBDNotificationObserver'

target :specs, :exclusive => true do
  link_with 'PBDNotificationObserverTests'
  pod 'OCHamcrest', '~> 4.0'
end
